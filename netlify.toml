[build]
  command = "npm run build"
  publish = "dist"
  SECRETS_SCAN_ENABLED = false

[functions]
  node_bundler = "esbuild"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/api"
  status = 200
